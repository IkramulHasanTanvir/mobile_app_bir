class TransactionsInfoModel {
  final String title, subtitle, imageUrl, titleName;

  TransactionsInfoModel({
    required this.titleName,
    required this.imageUrl,
    required this.title,
    required this.subtitle,
  });
}